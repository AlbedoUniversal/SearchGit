extends ../layout/main 

block variables
  - var pageClass = 'index-page';

block content 
  section(class="find") 
    h2(class="find-title") The search begins!
  section(class="inputs")
    input(class="inputs-search" type="text" value="")
    button(class="inputs-type" id="btnFind") find user
  section(class="cards")
  section(class="deletePrev")
    ul(id="paginations")
    button(class="deleteAll") clear field
